[profile.default]
src = "src"
out = "out"
libs = ["lib"]
script = "script"
test = "test"

# 合约本身是 0.8.30（交易产物里标注），脚本也用同版本编译最省事
solc_version = "0.8.30"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
# 运行时可用环境变量覆盖：BSC_RPC_URL=... forge script ...
bsc = "${BSC_RPC_URL}"

